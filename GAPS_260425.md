# Implementation Gaps — 2026-04-25

Status after ReWrite-1, ReWrite-2, and ReWrite-3.
Reference: `tree-sitter/lib/src/parser.h`, `crates/generate/src/render.rs`,
`crates/generate/src/build_tables/*.rs`.

---

## Current Remaining Gaps

### 1. Heavy C Parse-Table Construction Cost

`tree_sitter_c_json` now completes the routine compatibility path, including coarse
serialization, parser-table emission, parser.c emission, compatibility validation, and
compile-smoke. It is still expensive enough that it should stay in the heavy
compatibility step:

- serialize path is about 2-3 minutes locally
- peak RSS is roughly 3.4 GB
- it is not suitable for the fast default `zig build test` path

The next improvement should target parse-table memory pressure, especially item-set and
lookahead storage in coarse construction mode.

### 2. Parse-Table Pipeline Goldens Need EOF Refresh

Parser EOF/end-symbol work is implemented and covered by focused runtime tests, but the
older parse-table pipeline exact string goldens still reflect the pre-EOF dumps. The
compatibility-heavy step now filters to compatibility tests only, so these stale
pipeline goldens are isolated to `zig build test-pipeline`.

Refresh these goldens intentionally instead of treating the EOF actions as regressions.

### 3. Broader External-Scanner Runtime Parity

RW-3 proves a minimal external-scanner link path and sampled scanner-boundary behavior.
It does not yet claim complete parity for broad real external scanners with full
stateful scanner behavior across all runtime paths.

Future work should widen real external-scanner fixtures beyond the current sampled
boundary.

---

## No Longer Gaps

Closed by ReWrite-1 and ReWrite-2:

- ~~Parse table format~~ — large+small+actions implemented.
- ~~TSLanguage struct layout~~ — layout-compatible self-contained struct emitted.
- ~~Symbol, field, alias, supertype, reserved-word, and external-scanner tables~~ —
  serialized and emitted.
- ~~`ts_lex()` stub~~ — replaced by real NFA-to-DFA C emission.
- ~~`ts_lex_keywords()`~~ — emitted when `word_token` is present.
- ~~`ts_lex_modes[]` wiring~~ — emitted and wired into `TSLanguage`.

Closed by ReWrite-3:

- ~~End-to-end no-external runtime link test~~ — generated-from-grammar parser links
  with `../tree-sitter/lib/src` and accepts complete input at EOF.
- ~~Keyword/reserved-word runtime link test~~ — keyword capture and reserved-word
  lex-mode behavior are linked and exercised.
- ~~External-scanner compile/link test~~ — generated parser links with a minimal scanner
  implementation.
- ~~Shift action flags~~ — serialized shift actions carry `extra` and `repetition`.
- ~~Builder-level reserved-word-set propagation~~ — parse-table construction assigns
  and preserves non-zero reserved-word-set IDs.
- ~~EOF transitions in lexer model~~ — serialized lex states can carry real
  `eof_target` transitions.
- ~~Parser EOF/end symbol and accept actions~~ — EOF lookaheads produce EOF-keyed
  reduce/accept actions without becoming normal lexical tokens.
- ~~Inherited field metadata~~ — field map serialization/emission can set
  `inherited = true`.
- ~~Reduce/reduce precedence fallback~~ — local resolution follows the narrowed
  upstream-compatible rule.
- ~~Non-terminal extras~~ — supported for the current local boundary, with compile-smoke
  coverage.
- ~~Full compatibility compile-smoke shortlist~~ — `zig build test-compat-heavy` passes
  as a compatibility-only heavy step.
